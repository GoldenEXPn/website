from django.db import models
import datetime

# Create your models here.

# class React(models.Model):
#     email_title = models.CharField(max_length=100, default="No Subject")
#     sender = models.EmailField(default="no-reply@example.com")
#     content = models.TextField(default="No content provided")
#     received_date = models.DateTimeField(default=datetime.datetime(2024, 1, 1, 12, 0))

#     def __str__(self):
#         return self.email_title
